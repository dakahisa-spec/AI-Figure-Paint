{
  "name": "ai-figure-paint-gift-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "check": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute DB --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute DB --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260901.1",
    "typescript": "5.6.3",
    "wrangler": "4.127.1"
  }
}
